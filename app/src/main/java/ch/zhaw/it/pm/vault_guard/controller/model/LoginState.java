package ch.zhaw.it.pm.vault_guard.controller.model;

/**
 * The class LoginState is an enumeration of the possible login states.
 * The states are LOGOUT, LOGIN, FAIL, USERNAME_ERROR, ERROR.
 */
public enum LoginState {
    LOGOUT, LOGIN, FAIL, USERNAME_ERROR, ERROR
}
